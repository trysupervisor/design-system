"use client";
import { useTheme } from "@/components/theme-provider";
import { Badge } from "@/components/ui/badge";
export function Foundations() {
  const { theme, resolvedMode } = useTheme();
  const palette = resolvedMode === "dark" ? theme.dark : theme.light;
  return <><p className="eyebrow mb-4">FOUNDATIONS</p><h1 className="page-title">Theme foundations</h1><p className="page-description">Inspect the colors, typography, and dimensions of your current theme.</p><section className="mt-10"><div className="mb-5 flex items-center justify-between"><h2 className="text-lg font-semibold">Color</h2><Badge variant="outline">{theme.name}</Badge></div><div className="grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4">{Object.entries(palette).map(([key,value])=><div key={key} className="overflow-hidden rounded-lg border bg-card"><div className="h-16 border-b" style={{background:value}} /><div className="p-3"><p className="truncate text-xs font-medium">{key}</p><p className="mt-1 font-mono text-[10px] text-muted-foreground">{value}</p></div></div>)}</div></section><section className="mt-12 border-t pt-8"><h2 className="mb-6 text-lg font-semibold">Typography</h2><div className="panel p-6"><p className="mb-5 font-mono text-xs text-muted-foreground">{theme.font}</p><p className="type-sample">Aa Bb Cc</p><p className="mt-5 break-words text-xl tracking-tight">The quick brown fox jumps over the lazy dog.</p><p className="mt-5 font-mono text-lg">0123456789</p></div></section><section className="mt-12 border-t pt-8"><h2 className="mb-6 text-lg font-semibold">Shape and rhythm</h2><div className="grid grid-cols-2 gap-4 lg:grid-cols-4">{[["Corner radius",`${theme.radius * 16}px`],["Border width",`${theme.borderWidth}px`],["Spacing scale",`${theme.spacing}`],["Control height",`${theme.controlHeight * 16}px`]].map(([name,value])=><div key={name} className="panel p-5"><p className="text-xs text-muted-foreground">{name}</p><p className="mt-4 font-mono text-2xl">{value}</p></div>)}</div></section></>;
}
