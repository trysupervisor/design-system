import { ThemeStudio } from "@/components/theme-studio"

export const metadata = {
  title: "Theme Studio",
  description: "Build, preview, and export a theme for Supervisor components.",
}

export default function ThemesPage() {
  return <ThemeStudio />
}
